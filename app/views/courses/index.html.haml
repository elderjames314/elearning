%h1 Listing courses

Total course: 
= @courses.count

.card-columns
  - @courses.each do |course| 
    .card-column
      = render 'courses/course', course: course

%br

= link_to 'New Course', new_course_path
